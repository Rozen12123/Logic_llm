fact1
	foreach
		facts.White($x, True)
		facts.Blue($x, True)
	assert
		facts.Furry($x, True)

fact2
	foreach
		facts.Big($x, True)
		facts.White($x, True)
	assert
		facts.Smart($x, True)

fact3
	foreach
		facts.Furry($x, True)
	assert
		facts.Green($x, True)

fact4
	foreach
		facts.Kind($x, True)
	assert
		facts.Blue($x, True)

fact5
	foreach
		facts.Green($x, True)
	assert
		facts.Furry($x, True)

fact6
	foreach
		facts.Green($x, True)
	assert
		facts.Big($x, True)